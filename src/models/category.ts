export class Category {
    id: string;
    name: string;
    color: string;

    constructor(
        id,
        name,
        color
    ) {
        this.id = id;
        this.name = name;
        this.color = color;
    }
}
